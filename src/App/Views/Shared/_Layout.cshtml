@using Microsoft.AspNetCore.Hosting
@inject IHostingEnvironment HostingEnvironment

<!DOCTYPE html>
<html lang="nl"
      data-is-mobile="@BrowserDetector.IsMobileDevice().ToString().ToLowerInvariant()"
      data-app-version="@ViewBag.AppVersion">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>FinancialApp</title>

    <base href="/">

    <meta name="msapplication-config" content="/MSTileConfig.xml" />
    <link rel="apple-touch-icon" fa-href="/images/tiles/apple-tile.png" />

    <link href="/build/app.css" rel="stylesheet" asp-append-version="true"/>
</head>
<body data-bind="{ 'anonymous': !isAuthenticated(), 'authenticated': isAuthenticated }">
    @RenderBody()
</body>
</html>
